name: Setup Node
runs:
  using: "composite"
  steps:
    - uses: pnpm/action-setup@v2
      with:
        version: 8
    - uses: actions/setup-node@v4
      with:
        node-version: 18
        cache: pnpm
    - run: npm run corepack
      shell: bash
    - run: pnpm i
      shell: bash
